import os

// The OS module in python provides functions for interacting with the operating system

import sys

 // import sys in python is loading the module named sys into the current 
// namespace so that you can access the functions and anything else defined within the module using the module name.

import tensorflow as tf

// 

import cv2 as cv

import numpy as np 
